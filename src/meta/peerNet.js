// WebRTC peer stub — real P2P duels land later.

/**
 * @param {unknown} [_opts]
 * @returns {{ ok: false, reason: string }}
 */
export function connectPeer(_opts) {
  return { ok: false, reason: "coming soon" };
}

export function disconnectPeer() {
  /* stub */
}
